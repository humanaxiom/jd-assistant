"""jd_core — pure JD Bank domain contracts and logic (no I/O).

Ported near-verbatim from hris's ``packages/schemas`` / ``packages/pipeline``
(pydantic-only, low coupling). See ``docs/audit/hris-reuse-map.md``.
"""
