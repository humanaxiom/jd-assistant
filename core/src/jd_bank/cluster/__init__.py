"""Phase-3.5 role clustering — connected components over the admitted dedup edge
graph, plus the HR "eyeball" report. Report-only: persists nothing (the ``Cluster``
table is Phase 4). See :mod:`.runner` for the pass and :mod:`.report` for the artifacts.
"""
